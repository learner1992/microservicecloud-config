spring:
  profiles:
    active:
    - dev 
---
server:
  port: 18100
spring:
  profiles: dev
  application:
    name: airport-operation-access-control-system
  cloud:
    config:
      fail-fast: true
    consul:
      host: localhost
      port: 8500
      discovery:
        # instanceId: ${spring.application.name}:${server.port}:${random.value}
        instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
        healthCheckPath: /actuator/health
        healthCheckInterval: 5s
feign:
  hystrix:
    enabled: true
  


